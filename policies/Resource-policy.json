{
    "Version": "2012-10-17",
    "Statement": [
      {
        "Effect": "Allow",
        "Principal": "*",
        "Action": "${service_name}:*",
        "Resource": "arn:aws:${service_name}:${region}:${account_id}:*"
      }
    ]
  }
